export type Version = { name: string; version: string; gitCommit: string; buildDate: string; edition: string }
export type User = { id: string; username: string; displayName: string; email?: string; organizationId?: string; authMethod: string; isBootstrap: boolean; mustChangePassword: boolean; dataScope: string; permissions: string[]; csrfToken?: string }
export type AuthStatus = { localLoginEnabled: boolean; sso: { enabled: boolean; issuer?: string }; version: Version }
export type Customer = { id: string; name: string; registrationNo?: string; customerType: string; grade?: string; industry?: string; website?: string; phone?: string; email?: string; address?: string; ownerId: string; ownerName: string; health: string; annualRevenue?: number; employeeCount?: number; customFields: Record<string, unknown>; version: number; createdAt: string; updatedAt: string }
export type Stage = { id: string; pipelineId: string; name: string; order: number; probability: number; forecastCategory: string; isWon: boolean; isLost: boolean; color: string; minDays?: number; maxDays?: number }
export type Pipeline = { id: string; name: string; active: boolean; default: boolean; stages: Stage[] }
export type Opportunity = { id: string; name: string; customerId: string; customerName: string; ownerId: string; ownerName: string; pipelineId: string; stageId: string; stageName: string; stageColor: string; expectedAmount: number; currencyCode: string; exchangeRate: number; baseExpectedAmount: number; probability: number; weightedAmount: number; baseWeightedAmount: number; expectedCloseDate?: string; forecastCategory: string; competitor?: string; nextAction?: string; nextActionDate?: string; status: 'OPEN'|'WON'|'LOST'; health: string[]; version: number; updatedAt: string }
export type Activity = { id: string; customerId?: string; opportunityId?: string; activityType: string; subject: string; description?: string; occurredAt: string; nextAction?: string; nextActionDate?: string; ownerName: string }
export type Contract = { id:string; contractNo:string; customerId:string; customerName:string; opportunityId?:string; title:string; amount:number; currencyCode:string; exchangeRate:number; baseAmount:number; startDate?:string; endDate?:string; status:string; radarStatus:string; daysToExpiry?:number; autoRenew:boolean; revenueScheduleType:string; scheduleCount:number; recognizedCount:number; recognizedBaseAmount:number; renewalNoticeDays:number; renewalStatus:string; renewalAction?:string; activatedAt?:string; version:number; updatedAt:string }
export type RevenueSchedule = { id:string; contractId:string; sequenceNo:number; scheduledDate:string; amount:number; currencyCode:string; exchangeRate:number; baseAmount:number; status:string; recognizedSaleId?:string; recognizedAt?:string }
export type PersonalKey = { id: string; name: string; keyId: string; scopes: string[]; channels: string[]; status: string; expiresAt?: string; graceExpiresAt?: string; lastUsedAt?: string; lastUsedIp?: string; createdAt: string }
export type Approval = { id: string; policyId: string; policyName: string; entityType: string; entityId: string; requesterName: string; approverId?: string; approverName?: string; status: string; snapshot: Record<string, unknown>; reason?: string; requestedAt: string; version: number }
